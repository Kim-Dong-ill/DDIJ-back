import React, {Component} from 'react';
import PropTypes from 'prop-types';

class Auth extends Component {
    render() {
        return (
            <div>
                
            </div>
        );
    }
}

Auth.propTypes = {};

export default Auth;